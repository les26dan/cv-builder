export function logEvent(type: string, variantId: string) {
  console.log(`Event Type: ${type}, Variant ID: ${variantId}`);
  // Optionally, send this log to a backend service or store it in a database
} 